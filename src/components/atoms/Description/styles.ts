import styled from 'styled-components'; 

export const Text = styled.span`
    font-size: 0.8rem;
    &.dark {
      color: var(--dark)
    }
    &.primary {
      color: var(--primary)
    }
    &.gray {
      color: var(--gray)
    }
`