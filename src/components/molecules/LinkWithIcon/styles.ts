import styled from 'styled-components'; 

export const Container = styled.div`
  color: var(--white); 
  svg {
    font-size: 30px;
    margin-right: 0.5rem;
  }

  span {
    font-size: 1.2rem;
  }

  a {
    text-decoration: none;
    color: inherit;
    display: flex;
    align-items: center;
  }
`